// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"project-management-demo-backend/ent/favoriteproject"
	"project-management-demo-backend/ent/project"
	"project-management-demo-backend/ent/schema/ulid"
	"project-management-demo-backend/ent/teammate"
	"strings"
	"time"

	"entgo.io/ent/dialect/sql"
)

// FavoriteProject is the model entity for the FavoriteProject schema.
type FavoriteProject struct {
	config `json:"-"`
	// ID of the ent.
	ID ulid.ID `json:"id,omitempty"`
	// ProjectID holds the value of the "project_id" field.
	ProjectID ulid.ID `json:"project_id,omitempty"`
	// TeammateID holds the value of the "teammate_id" field.
	TeammateID ulid.ID `json:"teammate_id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the FavoriteProjectQuery when eager-loading is set.
	Edges FavoriteProjectEdges `json:"edges"`
}

// FavoriteProjectEdges holds the relations/edges for other nodes in the graph.
type FavoriteProjectEdges struct {
	// Project holds the value of the project edge.
	Project *Project `json:"project,omitempty"`
	// Teammate holds the value of the teammate edge.
	Teammate *Teammate `json:"teammate,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// ProjectOrErr returns the Project value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e FavoriteProjectEdges) ProjectOrErr() (*Project, error) {
	if e.loadedTypes[0] {
		if e.Project == nil {
			// The edge project was loaded in eager-loading,
			// but was not found.
			return nil, &NotFoundError{label: project.Label}
		}
		return e.Project, nil
	}
	return nil, &NotLoadedError{edge: "project"}
}

// TeammateOrErr returns the Teammate value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e FavoriteProjectEdges) TeammateOrErr() (*Teammate, error) {
	if e.loadedTypes[1] {
		if e.Teammate == nil {
			// The edge teammate was loaded in eager-loading,
			// but was not found.
			return nil, &NotFoundError{label: teammate.Label}
		}
		return e.Teammate, nil
	}
	return nil, &NotLoadedError{edge: "teammate"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*FavoriteProject) scanValues(columns []string) ([]interface{}, error) {
	values := make([]interface{}, len(columns))
	for i := range columns {
		switch columns[i] {
		case favoriteproject.FieldCreatedAt, favoriteproject.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case favoriteproject.FieldID, favoriteproject.FieldProjectID, favoriteproject.FieldTeammateID:
			values[i] = new(ulid.ID)
		default:
			return nil, fmt.Errorf("unexpected column %q for type FavoriteProject", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the FavoriteProject fields.
func (fp *FavoriteProject) assignValues(columns []string, values []interface{}) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case favoriteproject.FieldID:
			if value, ok := values[i].(*ulid.ID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				fp.ID = *value
			}
		case favoriteproject.FieldProjectID:
			if value, ok := values[i].(*ulid.ID); !ok {
				return fmt.Errorf("unexpected type %T for field project_id", values[i])
			} else if value != nil {
				fp.ProjectID = *value
			}
		case favoriteproject.FieldTeammateID:
			if value, ok := values[i].(*ulid.ID); !ok {
				return fmt.Errorf("unexpected type %T for field teammate_id", values[i])
			} else if value != nil {
				fp.TeammateID = *value
			}
		case favoriteproject.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				fp.CreatedAt = value.Time
			}
		case favoriteproject.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				fp.UpdatedAt = value.Time
			}
		}
	}
	return nil
}

// QueryProject queries the "project" edge of the FavoriteProject entity.
func (fp *FavoriteProject) QueryProject() *ProjectQuery {
	return (&FavoriteProjectClient{config: fp.config}).QueryProject(fp)
}

// QueryTeammate queries the "teammate" edge of the FavoriteProject entity.
func (fp *FavoriteProject) QueryTeammate() *TeammateQuery {
	return (&FavoriteProjectClient{config: fp.config}).QueryTeammate(fp)
}

// Update returns a builder for updating this FavoriteProject.
// Note that you need to call FavoriteProject.Unwrap() before calling this method if this FavoriteProject
// was returned from a transaction, and the transaction was committed or rolled back.
func (fp *FavoriteProject) Update() *FavoriteProjectUpdateOne {
	return (&FavoriteProjectClient{config: fp.config}).UpdateOne(fp)
}

// Unwrap unwraps the FavoriteProject entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (fp *FavoriteProject) Unwrap() *FavoriteProject {
	tx, ok := fp.config.driver.(*txDriver)
	if !ok {
		panic("ent: FavoriteProject is not a transactional entity")
	}
	fp.config.driver = tx.drv
	return fp
}

// String implements the fmt.Stringer.
func (fp *FavoriteProject) String() string {
	var builder strings.Builder
	builder.WriteString("FavoriteProject(")
	builder.WriteString(fmt.Sprintf("id=%v", fp.ID))
	builder.WriteString(", project_id=")
	builder.WriteString(fmt.Sprintf("%v", fp.ProjectID))
	builder.WriteString(", teammate_id=")
	builder.WriteString(fmt.Sprintf("%v", fp.TeammateID))
	builder.WriteString(", created_at=")
	builder.WriteString(fp.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", updated_at=")
	builder.WriteString(fp.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// FavoriteProjects is a parsable slice of FavoriteProject.
type FavoriteProjects []*FavoriteProject

func (fp FavoriteProjects) config(cfg config) {
	for _i := range fp {
		fp[_i].config = cfg
	}
}
