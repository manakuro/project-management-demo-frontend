// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"project-management-demo-backend/ent/favoriteworkspace"
	"project-management-demo-backend/ent/schema/ulid"
	"project-management-demo-backend/ent/teammate"
	"project-management-demo-backend/ent/workspace"
	"strings"
	"time"

	"entgo.io/ent/dialect/sql"
)

// FavoriteWorkspace is the model entity for the FavoriteWorkspace schema.
type FavoriteWorkspace struct {
	config `json:"-"`
	// ID of the ent.
	ID ulid.ID `json:"id,omitempty"`
	// WorkspaceID holds the value of the "workspace_id" field.
	WorkspaceID ulid.ID `json:"workspace_id,omitempty"`
	// TeammateID holds the value of the "teammate_id" field.
	TeammateID ulid.ID `json:"teammate_id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the FavoriteWorkspaceQuery when eager-loading is set.
	Edges FavoriteWorkspaceEdges `json:"edges"`
}

// FavoriteWorkspaceEdges holds the relations/edges for other nodes in the graph.
type FavoriteWorkspaceEdges struct {
	// Workspace holds the value of the workspace edge.
	Workspace *Workspace `json:"workspace,omitempty"`
	// Teammate holds the value of the teammate edge.
	Teammate *Teammate `json:"teammate,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// WorkspaceOrErr returns the Workspace value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e FavoriteWorkspaceEdges) WorkspaceOrErr() (*Workspace, error) {
	if e.loadedTypes[0] {
		if e.Workspace == nil {
			// The edge workspace was loaded in eager-loading,
			// but was not found.
			return nil, &NotFoundError{label: workspace.Label}
		}
		return e.Workspace, nil
	}
	return nil, &NotLoadedError{edge: "workspace"}
}

// TeammateOrErr returns the Teammate value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e FavoriteWorkspaceEdges) TeammateOrErr() (*Teammate, error) {
	if e.loadedTypes[1] {
		if e.Teammate == nil {
			// The edge teammate was loaded in eager-loading,
			// but was not found.
			return nil, &NotFoundError{label: teammate.Label}
		}
		return e.Teammate, nil
	}
	return nil, &NotLoadedError{edge: "teammate"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*FavoriteWorkspace) scanValues(columns []string) ([]interface{}, error) {
	values := make([]interface{}, len(columns))
	for i := range columns {
		switch columns[i] {
		case favoriteworkspace.FieldCreatedAt, favoriteworkspace.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case favoriteworkspace.FieldID, favoriteworkspace.FieldWorkspaceID, favoriteworkspace.FieldTeammateID:
			values[i] = new(ulid.ID)
		default:
			return nil, fmt.Errorf("unexpected column %q for type FavoriteWorkspace", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the FavoriteWorkspace fields.
func (fw *FavoriteWorkspace) assignValues(columns []string, values []interface{}) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case favoriteworkspace.FieldID:
			if value, ok := values[i].(*ulid.ID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				fw.ID = *value
			}
		case favoriteworkspace.FieldWorkspaceID:
			if value, ok := values[i].(*ulid.ID); !ok {
				return fmt.Errorf("unexpected type %T for field workspace_id", values[i])
			} else if value != nil {
				fw.WorkspaceID = *value
			}
		case favoriteworkspace.FieldTeammateID:
			if value, ok := values[i].(*ulid.ID); !ok {
				return fmt.Errorf("unexpected type %T for field teammate_id", values[i])
			} else if value != nil {
				fw.TeammateID = *value
			}
		case favoriteworkspace.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				fw.CreatedAt = value.Time
			}
		case favoriteworkspace.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				fw.UpdatedAt = value.Time
			}
		}
	}
	return nil
}

// QueryWorkspace queries the "workspace" edge of the FavoriteWorkspace entity.
func (fw *FavoriteWorkspace) QueryWorkspace() *WorkspaceQuery {
	return (&FavoriteWorkspaceClient{config: fw.config}).QueryWorkspace(fw)
}

// QueryTeammate queries the "teammate" edge of the FavoriteWorkspace entity.
func (fw *FavoriteWorkspace) QueryTeammate() *TeammateQuery {
	return (&FavoriteWorkspaceClient{config: fw.config}).QueryTeammate(fw)
}

// Update returns a builder for updating this FavoriteWorkspace.
// Note that you need to call FavoriteWorkspace.Unwrap() before calling this method if this FavoriteWorkspace
// was returned from a transaction, and the transaction was committed or rolled back.
func (fw *FavoriteWorkspace) Update() *FavoriteWorkspaceUpdateOne {
	return (&FavoriteWorkspaceClient{config: fw.config}).UpdateOne(fw)
}

// Unwrap unwraps the FavoriteWorkspace entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (fw *FavoriteWorkspace) Unwrap() *FavoriteWorkspace {
	tx, ok := fw.config.driver.(*txDriver)
	if !ok {
		panic("ent: FavoriteWorkspace is not a transactional entity")
	}
	fw.config.driver = tx.drv
	return fw
}

// String implements the fmt.Stringer.
func (fw *FavoriteWorkspace) String() string {
	var builder strings.Builder
	builder.WriteString("FavoriteWorkspace(")
	builder.WriteString(fmt.Sprintf("id=%v", fw.ID))
	builder.WriteString(", workspace_id=")
	builder.WriteString(fmt.Sprintf("%v", fw.WorkspaceID))
	builder.WriteString(", teammate_id=")
	builder.WriteString(fmt.Sprintf("%v", fw.TeammateID))
	builder.WriteString(", created_at=")
	builder.WriteString(fw.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", updated_at=")
	builder.WriteString(fw.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// FavoriteWorkspaces is a parsable slice of FavoriteWorkspace.
type FavoriteWorkspaces []*FavoriteWorkspace

func (fw FavoriteWorkspaces) config(cfg config) {
	for _i := range fw {
		fw[_i].config = cfg
	}
}
