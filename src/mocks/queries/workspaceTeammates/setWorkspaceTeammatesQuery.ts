// Generated by `yarn new:queryMock`
import { server } from '../../server'
import { workspaceTeammatesQuery } from '../workspaceTeammates'
import { Options } from './type'

export const setWorkspaceTeammatesQuery = (options: Options) => {
  server.use(workspaceTeammatesQuery(options))
}
