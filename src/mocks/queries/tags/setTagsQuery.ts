// Generated by `yarn new:queryMock`
import { server } from '../../server'
import { tagsQuery } from '../tags'
import { Options } from './type'

export const setTagsQuery = (options: Options) => {
  server.use(tagsQuery(options))
}
