// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"project-management-demo-backend/ent/color"
	"project-management-demo-backend/ent/projectbasecolor"
	"project-management-demo-backend/ent/schema/ulid"
	"strings"
	"time"

	"entgo.io/ent/dialect/sql"
)

// ProjectBaseColor is the model entity for the ProjectBaseColor schema.
type ProjectBaseColor struct {
	config `json:"-"`
	// ID of the ent.
	ID ulid.ID `json:"id,omitempty"`
	// ColorID holds the value of the "color_id" field.
	ColorID ulid.ID `json:"color_id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ProjectBaseColorQuery when eager-loading is set.
	Edges ProjectBaseColorEdges `json:"edges"`
}

// ProjectBaseColorEdges holds the relations/edges for other nodes in the graph.
type ProjectBaseColorEdges struct {
	// Projects holds the value of the projects edge.
	Projects []*Project `json:"projects,omitempty"`
	// Color holds the value of the color edge.
	Color *Color `json:"color,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// ProjectsOrErr returns the Projects value or an error if the edge
// was not loaded in eager-loading.
func (e ProjectBaseColorEdges) ProjectsOrErr() ([]*Project, error) {
	if e.loadedTypes[0] {
		return e.Projects, nil
	}
	return nil, &NotLoadedError{edge: "projects"}
}

// ColorOrErr returns the Color value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ProjectBaseColorEdges) ColorOrErr() (*Color, error) {
	if e.loadedTypes[1] {
		if e.Color == nil {
			// The edge color was loaded in eager-loading,
			// but was not found.
			return nil, &NotFoundError{label: color.Label}
		}
		return e.Color, nil
	}
	return nil, &NotLoadedError{edge: "color"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ProjectBaseColor) scanValues(columns []string) ([]interface{}, error) {
	values := make([]interface{}, len(columns))
	for i := range columns {
		switch columns[i] {
		case projectbasecolor.FieldCreatedAt, projectbasecolor.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case projectbasecolor.FieldID, projectbasecolor.FieldColorID:
			values[i] = new(ulid.ID)
		default:
			return nil, fmt.Errorf("unexpected column %q for type ProjectBaseColor", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ProjectBaseColor fields.
func (pbc *ProjectBaseColor) assignValues(columns []string, values []interface{}) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case projectbasecolor.FieldID:
			if value, ok := values[i].(*ulid.ID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				pbc.ID = *value
			}
		case projectbasecolor.FieldColorID:
			if value, ok := values[i].(*ulid.ID); !ok {
				return fmt.Errorf("unexpected type %T for field color_id", values[i])
			} else if value != nil {
				pbc.ColorID = *value
			}
		case projectbasecolor.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				pbc.CreatedAt = value.Time
			}
		case projectbasecolor.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				pbc.UpdatedAt = value.Time
			}
		}
	}
	return nil
}

// QueryProjects queries the "projects" edge of the ProjectBaseColor entity.
func (pbc *ProjectBaseColor) QueryProjects() *ProjectQuery {
	return (&ProjectBaseColorClient{config: pbc.config}).QueryProjects(pbc)
}

// QueryColor queries the "color" edge of the ProjectBaseColor entity.
func (pbc *ProjectBaseColor) QueryColor() *ColorQuery {
	return (&ProjectBaseColorClient{config: pbc.config}).QueryColor(pbc)
}

// Update returns a builder for updating this ProjectBaseColor.
// Note that you need to call ProjectBaseColor.Unwrap() before calling this method if this ProjectBaseColor
// was returned from a transaction, and the transaction was committed or rolled back.
func (pbc *ProjectBaseColor) Update() *ProjectBaseColorUpdateOne {
	return (&ProjectBaseColorClient{config: pbc.config}).UpdateOne(pbc)
}

// Unwrap unwraps the ProjectBaseColor entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (pbc *ProjectBaseColor) Unwrap() *ProjectBaseColor {
	tx, ok := pbc.config.driver.(*txDriver)
	if !ok {
		panic("ent: ProjectBaseColor is not a transactional entity")
	}
	pbc.config.driver = tx.drv
	return pbc
}

// String implements the fmt.Stringer.
func (pbc *ProjectBaseColor) String() string {
	var builder strings.Builder
	builder.WriteString("ProjectBaseColor(")
	builder.WriteString(fmt.Sprintf("id=%v", pbc.ID))
	builder.WriteString(", color_id=")
	builder.WriteString(fmt.Sprintf("%v", pbc.ColorID))
	builder.WriteString(", created_at=")
	builder.WriteString(pbc.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", updated_at=")
	builder.WriteString(pbc.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// ProjectBaseColors is a parsable slice of ProjectBaseColor.
type ProjectBaseColors []*ProjectBaseColor

func (pbc ProjectBaseColors) config(cfg config) {
	for _i := range pbc {
		pbc[_i].config = cfg
	}
}
