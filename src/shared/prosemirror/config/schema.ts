import { Schema } from 'prosemirror-model'
import {
  blockquote,
  bold,
  code,
  codeBlock,
  doc,
  emoji,
  heading,
  horizontalRule,
  italic,
  lineBreak,
  link,
  list,
  listItem,
  mention,
  paragraph,
  strikethrough,
  subscript,
  superscript,
  table,
  tableDataCell,
  tableHeaderCell,
  tableRow,
  text,
  underline,
} from '../schema'

export const schema = new Schema({
  marks: {
    bold,
    code,
    italic,
    link,
    strikethrough,
    subscript,
    superscript,
    underline,
  },
  nodes: {
    text,
    doc,
    paragraph,
    lineBreak,
    heading,
    blockquote,
    codeBlock,
    horizontalRule,
    list,
    listItem,
    table,
    tableRow,
    tableDataCell,
    tableHeaderCell,
    mention,
    emoji,
  },
})
