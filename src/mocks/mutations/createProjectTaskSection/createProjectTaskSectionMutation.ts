// Generated by `yarn new:mutationMock`
import { graphql } from 'msw';
import { data } from './data';
import type { Mutation, Options } from './type';

export const createProjectTaskSectionMutation = (options?: Options) => {
  return graphql.mutation<Mutation>(
    'CreateProjectTaskSection',
    (_, res, ctx) => {
      if (options?.networkError) {
        return res(
          ctx.errors([
            {
              message: 'Network request failed',
              graphQLErrors: [],
              networkError: new Error('error'),
              errorMessage: 'error',
              extraInfo: {},
            },
          ]),
        );
      }

      return res(ctx.data(data(options?.res, options?.deepMergeOptions)));
    },
  );
};
