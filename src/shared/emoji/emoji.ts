import { Picker } from 'emoji-mart'

export type { BaseEmoji, EmojiData, EmojiSkin } from 'emoji-mart'
export { emojiIndex as emojiData, frequently } from 'emoji-mart'

export const EmojiPicker = Picker
