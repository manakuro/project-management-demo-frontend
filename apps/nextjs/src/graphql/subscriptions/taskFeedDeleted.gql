subscription TaskFeedDeleted($workspaceId: ID!, $requestId: String!) {
  taskFeedDeleted(workspaceId: $workspaceId, requestId: $requestId) {
    taskFeed {
      ...TaskFeedFragment
    }
    taskFiles {
      ...TaskFileFragment
    }
    taskFeedLikes {
      ...TaskFeedLikeFragment
    }
  }
}
