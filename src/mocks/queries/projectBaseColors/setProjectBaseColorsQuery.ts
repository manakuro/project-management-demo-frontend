// Generated by `yarn new:queryMock`
import { server } from '../../server';
import { projectBaseColorsQuery } from '../projectBaseColors';
import type { Options } from './type';

export const setProjectBaseColorsQuery = (options: Options) => {
  server.use(projectBaseColorsQuery(options));
};
