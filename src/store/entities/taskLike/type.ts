export type TaskLikeResponse = {
  id: string
  taskId: string
  teammateId: string
  createdAt: string
  updatedAt: string
}

export type TaskLike = {
  id: string
  taskId: string
  teammateId: string
  createdAt: string
  updatedAt: string
}
