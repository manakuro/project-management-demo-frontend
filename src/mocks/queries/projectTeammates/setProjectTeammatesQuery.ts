// Generated by `yarn new:queryMock`
import { server } from '../../server'
import { projectTeammatesQuery } from '../projectTeammates'
import { Options } from './type'

export const setProjectTeammatesQuery = (options: Options) => {
  server.use(projectTeammatesQuery(options))
}
