// Generated by `yarn new:queryMock`
import { server } from '../../server'
import { taskQuery } from '../task'
import type { Options } from './type'

export const setTaskQuery = (options: Options) => {
  server.use(taskQuery(options))
}
